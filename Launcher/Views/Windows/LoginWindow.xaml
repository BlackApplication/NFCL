<views:MvxWindow
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    x:Class="Launcher.Views.Windows.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Launcher.Views.Windows"
    xmlns:components="clr-namespace:Launcher.Views.Components"
    xmlns:vm="clr-namespace:Core.ViewModels;assembly=Core"
    mc:Ignorable="d"
    d:DataContext="{ d:DesignInstance Type=vm:LoginViewModel }"
    WindowStyle="None"
    WindowStartupLocation="Manual"
    AllowsTransparency="True"
    Background="Transparent"
    Width="350" Height="400">
    <Border Width="350"
            Height="400"
            HorizontalAlignment="Center"
            BorderBrush="Black"
            BorderThickness="1"
            Style="{ StaticResource BorderRounding10 }">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.RowSpan="6"
                Background="#ACACAC"
                Opacity="0.8"
                Style="{ StaticResource BorderRounding10 }"/>

            <Grid Grid.Row="0">
                <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="Вход"
                       FontSize="40"
                       Foreground="Black"/>
            </Grid>

            <components:ImageInput Grid.Row="1" InputText="{ Binding Email }" ImagePath="/Resources/email.png" Height="40" Width="300"/>

            <components:ImageInputPass Grid.Row="2" Password="{ Binding Password }" ImagePath="/Resources/lock.png" Height="40" Width="300"/>

            <Grid Grid.Row="3" Width="300" Visibility="{ Binding ErrorVisible }">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="42"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="pack://application:,,,/Resources/invalid.png" Width="20" Height="20" Margin="11,19,11,25"/>

                <TextBlock Grid.Column="1" Foreground="#E15454" Text="Такого пользователя несуществует или пароль неверный!"  VerticalAlignment="Top" FontSize="14" TextWrapping="Wrap" Margin="0,10,0,0"/>
            </Grid>

            <Grid Grid.Row="4">
                <Grid Width="200" HorizontalAlignment="Left" Margin="5, 0, 0, 0">
                    <Grid.RowDefinitions>
                        <RowDefinition></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" VerticalAlignment="Bottom" Margin="18, 0, 0, 0" FontSize="18" Foreground="White">Нет аккаунта?</TextBlock>
                    <Button Grid.Row="1" Command="{ Binding GoToSiteCommand }" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Style="{ StaticResource CommonButtonStyle }" FontSize="18" Foreground="White">
                        <TextBlock TextDecorations="Underline" Text="зарегестрироваться"/>
                    </Button>
                </Grid>
            </Grid>

            <Grid Grid.Row="5">
                <Border Style="{ StaticResource BorderRounding15 }" Background="#BF61D7" Width="90" Height="40" Margin="225,10,35,15">
                    <Button Style="{ StaticResource CommonButtonStyle }" Background="Transparent" BorderThickness="0" Margin="0, 0, 0, 5" Command="{ Binding LoginCommand }">
                        <TextBlock Text="войти" FontSize="24" Foreground="White"/>
                    </Button>
                </Border>
            </Grid>
        </Grid>
    </Border>
</views:MvxWindow>
